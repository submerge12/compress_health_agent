node_modules/
dist/
coverage/
.env
.env.*
!.env.example
_tmp_*
.ai/
.codex-test-tmp/
.codex/config.toml
*.stackdump
.serve.log

# AOH board raw logs (manifests go under evidence/, raw logs stay local)
.evidence-local/

# Local agent tooling (Claude Code config, skills)
.agents/
.claude/
skills-lock.json

# pi-harness local session data (created when running the CLI)
.pi-harness/
